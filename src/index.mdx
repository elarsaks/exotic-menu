---
name: Menu Component
route: /
---

import { Playground, Props } from 'docz'
import { Menu } from './components/Menu.tsx'
import { Home, Info, SettingsApplications, Collections, ContactMail } from '@material-ui/icons'
import t from 'prop-types'

## Properties

<Props of={Menu} />

## Basic usage

<Playground>
{() => {
    const items = [
      {
        name: 'Home',
        route: '/home',
        icon: Home,
      },
      {
        name: 'Services',
        route: '/services',
        icon: SettingsApplications,
      },
      {
        name: 'Projects',
        route: '/projects',
        icon: Collections,
      },
      {
        name: 'About',
        route: '/about',
        icon: Info,
      },
      {
        name: 'Contact',
        route: '/contact',
        icon: ContactMail,
      }
    ]
    const [route, setRoute] = React.useState('/about')
    return (
      <>
        <div style={{height:'300px', textAlign:'center', fontSize: '3em'}}>"{route}"</div>
        <Menu items={items} handleRouting={setRoute}/>
      </>
    )
  }}
</Playground>
